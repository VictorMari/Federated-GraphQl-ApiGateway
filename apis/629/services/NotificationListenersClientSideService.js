/* eslint-disable no-unused-vars */
const Service = require('./Service');

  /**
   * Client listener for entity CustomerAttributeValueChangeEvent
   * Example of a client listener for receiving the notification CustomerAttributeValueChangeEvent
   *
   * data CustomerAttributeValueChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToCustomerAttributeValueChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToCustomerAttributeValueChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToCustomerAttributeValueChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity CustomerCreateEvent
   * Example of a client listener for receiving the notification CustomerCreateEvent
   *
   * data CustomerCreateEvent The event data
   * returns EventSubscription
   **/
  const listenToCustomerCreateEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToCustomerCreateEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToCustomerCreateEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity CustomerDeleteEvent
   * Example of a client listener for receiving the notification CustomerDeleteEvent
   *
   * data CustomerDeleteEvent The event data
   * returns EventSubscription
   **/
  const listenToCustomerDeleteEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToCustomerDeleteEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToCustomerDeleteEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity CustomerStateChangeEvent
   * Example of a client listener for receiving the notification CustomerStateChangeEvent
   *
   * data CustomerStateChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToCustomerStateChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToCustomerStateChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToCustomerStateChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    

module.exports = {
  listenToCustomerAttributeValueChangeEvent,
  listenToCustomerCreateEvent,
  listenToCustomerDeleteEvent,
  listenToCustomerStateChangeEvent,
};
