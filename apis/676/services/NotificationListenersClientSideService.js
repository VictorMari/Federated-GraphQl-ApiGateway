/* eslint-disable no-unused-vars */
const Service = require('./Service');

  /**
   * Client listener for entity PaymentAttributeValueChangeEvent
   * Example of a client listener for receiving the notification PaymentAttributeValueChangeEvent
   *
   * data PaymentAttributeValueChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToPaymentAttributeValueChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToPaymentAttributeValueChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToPaymentAttributeValueChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity PaymentCreateEvent
   * Example of a client listener for receiving the notification PaymentCreateEvent
   *
   * data PaymentCreateEvent The event data
   * returns EventSubscription
   **/
  const listenToPaymentCreateEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToPaymentCreateEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToPaymentCreateEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity PaymentDeleteEvent
   * Example of a client listener for receiving the notification PaymentDeleteEvent
   *
   * data PaymentDeleteEvent The event data
   * returns EventSubscription
   **/
  const listenToPaymentDeleteEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToPaymentDeleteEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToPaymentDeleteEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity PaymentStateChangeEvent
   * Example of a client listener for receiving the notification PaymentStateChangeEvent
   *
   * data PaymentStateChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToPaymentStateChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToPaymentStateChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToPaymentStateChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity RefundAttributeValueChangeEvent
   * Example of a client listener for receiving the notification RefundAttributeValueChangeEvent
   *
   * data RefundAttributeValueChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToRefundAttributeValueChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToRefundAttributeValueChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToRefundAttributeValueChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity RefundCreateEvent
   * Example of a client listener for receiving the notification RefundCreateEvent
   *
   * data RefundCreateEvent The event data
   * returns EventSubscription
   **/
  const listenToRefundCreateEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToRefundCreateEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToRefundCreateEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity RefundDeleteEvent
   * Example of a client listener for receiving the notification RefundDeleteEvent
   *
   * data RefundDeleteEvent The event data
   * returns EventSubscription
   **/
  const listenToRefundDeleteEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToRefundDeleteEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToRefundDeleteEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity RefundStateChangeEvent
   * Example of a client listener for receiving the notification RefundStateChangeEvent
   *
   * data RefundStateChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToRefundStateChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToRefundStateChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToRefundStateChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    

module.exports = {
  listenToPaymentAttributeValueChangeEvent,
  listenToPaymentCreateEvent,
  listenToPaymentDeleteEvent,
  listenToPaymentStateChangeEvent,
  listenToRefundAttributeValueChangeEvent,
  listenToRefundCreateEvent,
  listenToRefundDeleteEvent,
  listenToRefundStateChangeEvent,
};
