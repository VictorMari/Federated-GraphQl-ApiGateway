/* eslint-disable no-unused-vars */
const Service = require('./Service');

  /**
   * Client listener for entity ProductAttributeValueChangeEvent
   * Example of a client listener for receiving the notification ProductAttributeValueChangeEvent
   *
   * data ProductAttributeValueChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToProductAttributeValueChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToProductAttributeValueChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToProductAttributeValueChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity ProductBatchEvent
   * Example of a client listener for receiving the notification ProductBatchEvent
   *
   * data ProductBatchEvent The event data
   * returns EventSubscription
   **/
  const listenToProductBatchEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToProductBatchEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToProductBatchEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity ProductCreateEvent
   * Example of a client listener for receiving the notification ProductCreateEvent
   *
   * data ProductCreateEvent The event data
   * returns EventSubscription
   **/
  const listenToProductCreateEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToProductCreateEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToProductCreateEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity ProductDeleteEvent
   * Example of a client listener for receiving the notification ProductDeleteEvent
   *
   * data ProductDeleteEvent The event data
   * returns EventSubscription
   **/
  const listenToProductDeleteEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToProductDeleteEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToProductDeleteEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    
  /**
   * Client listener for entity ProductStateChangeEvent
   * Example of a client listener for receiving the notification ProductStateChangeEvent
   *
   * data ProductStateChangeEvent The event data
   * returns EventSubscription
   **/
  const listenToProductStateChangeEvent =  ( args, context /* data  */) =>
    new Promise(
      async (resolve) => {
        context.classname    = "NotificationListenersClientSide";
        context.operationId  = "listenToProductStateChangeEvent";
        context.method       = "post";
        try {
          /* NOT matching isRestful */
          resolve(Service.serve(args, context ));

        } catch (e) {
          console.log("listenToProductStateChangeEvent: error=" + e);
          resolve(Service.rejectResponse(
            e.message || 'Invalid input',
            e.status || 405,
          ));
        }
      },
    )
    

module.exports = {
  listenToProductAttributeValueChangeEvent,
  listenToProductBatchEvent,
  listenToProductCreateEvent,
  listenToProductDeleteEvent,
  listenToProductStateChangeEvent,
};
